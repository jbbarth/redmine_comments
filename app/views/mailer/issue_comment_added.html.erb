<%= l(:text_issue_updated, :id => "##{@issue.id}", :author => h(@comment.author)) %>

<%= textilizable(@comment.comments, :only_path => false) %>

<hr />

<%= render :partial => 'issue', :formats => [:html], :locals => { :issue => @issue, :issue_url => @issue_url, :users => @recipients } %>
